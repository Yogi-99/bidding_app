import 'package:flutter/material.dart';

class InfinityScreen extends StatelessWidget {
  static String id = 'infinity_screen';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Infinity Screen'),
      ),
    );
  }
}
